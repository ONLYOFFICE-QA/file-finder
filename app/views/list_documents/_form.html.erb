<%= form_for(@list_document) do |f| %>
    <% if @list_document.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@list_document.errors.count, "error") %> prohibited this list_document from being
            saved:</h2>

          <ul>
            <% @list_document.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="field">
      <%= f.label :path %>
      <br>
      <%= f.text_field :path, size: 50 %>
    </div>
    <div class="actions">
      <%= f.submit('Add document') %>
    </div>
<% end %>
