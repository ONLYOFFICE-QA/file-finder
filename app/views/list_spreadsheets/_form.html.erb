<%= form_for(@list_spreadsheet) do |f| %>
  <% if @list_spreadsheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_spreadsheet.errors.count, "error") %> prohibited this list_spreadsheet from being saved:</h2>

      <ul>
      <% @list_spreadsheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
